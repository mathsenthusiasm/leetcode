#include<stdio.h>
zeros_last(int nums[],int n){
    int left=0;
    for(int right=0;right<n;right++){
        if(nums[i]!=0){
             
        }
    }
}
int main(){
    int nums[]={0,1,0,3,12,0};
    int n=sizeof(nums)/sizeof(nums[0]);
    zeros_last(nums,n);
}