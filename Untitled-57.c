#include<stdio.h>
int rotate_array(int arr[],int k,int n){
    reverse(arr,0,n);
    reverse(arr,)
}
int main(){
    int arr[]={2,3,5,7,9};
    int k=2;
    int n=sizeof(arr)/sizeof(arr[0]);
    printf("elements of rotate array:");

    rotate_array(arr,k,n);
}