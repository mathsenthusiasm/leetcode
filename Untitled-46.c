#include<stdio.h>
void zeros_ones(int arr[],int n){
    int left=0,right=n-1;
    if(left<n && right<n) 
        if(arr[i]==0)
}
int main(){
    arr[]={1,0,1,0,0,1,1};
    int n=sizeof(arr)/sizeof(arr[0]);
    printf("arrays element:");
zeros_ones(arr,n);
}